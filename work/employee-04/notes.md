# Notes

- No conflicts found between context files.
- Assumed internal vs external classification is based on organization domain list.
- Assumed a single SMTP provider with inbound webhooks is acceptable for v0 MVP transport.
- Assumed v0 delay window is fixed to 5–30 minutes per PM prompt and stored for audit.
